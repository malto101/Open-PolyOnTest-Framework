[package]
name = "polytest-rs"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rust language adapter for PolyTest (calls the C ABI)"

[features]
default = []
std = []

[dependencies]

[lib]
name = "polytest_rs"
path = "src/lib.rs"
